FPing is a tool that I have only used for quick ICMP subnet sweeps and host identification. The syntax for this command is as follows:

```shell
fping -asgq <subnet in CIDR>
```

Install with apt on Kali
